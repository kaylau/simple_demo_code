<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kay.demo.kotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.FirstActivity" />
        <activity android:name=".recycleview.activitis.RecycleActivity" />
        <activity android:name=".viewpager.ViewPagerActivity" />
        <activity android:name=".viewpager.TabLayoutActivity" />
        <activity android:name=".viewpager.CustomIndicatorActivity" />
        <activity android:name=".recycleview.activitis.ScrollRecycleActivity" />
        <activity android:name=".demo.HomeJDActivity" />
        <activity android:name=".demo.JDHomeActivity" />
        <activity android:name=".demo.RvRefreshActivity" />
    </application>

</manifest>